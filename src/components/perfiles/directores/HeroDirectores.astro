---
/**
 * Hero Directores: franja 80% (#D2E6FF), dos columnas (copy + imagen), CTA demo + plataforma (menú 3 niveles, patrón Header).
 * Estructura / diagramación / responsive alineados a HeroPreescolar (sin carrusel).
 * Imagen: public/images/perfiles/directores/image-hero.webp
 */
const heroImageSrc = '/images/perfiles/directores/image-hero.webp';
---

<section class="perfil-directores-hero">
  <div class="perfil-directores-hero__strip">
    <div class="perfil-directores-hero__inner layout-container">
      <div class="perfil-directores-hero__row">
        <header class="perfil-directores-hero__content">
          <p class="programas__label programas__label--align-start">
            <span class="programas__label-dot" aria-hidden="true"></span>
            Directores
          </p>
          <h1 id="perfil-directores-hero-title" class="perfil-directores-hero__title text-h1">
            Toma mejores decisiones, todos los días con Luca<span class="perfil-directores-hero__title-dot" aria-hidden="true">.</span>
          </h1>
          <p class="perfil-directores-hero__subtitle text-subtitle-1">
            Solución educativa para colegios en México
          </p>
          <div class="perfil-directores-hero__ctas">
            <a href="/contacto" class="btn-base btn-primary btn-cta-animated">
              <span>Solicita tu demo</span>
              <span class="perfil-directores-hero__btn-icon" aria-hidden="true"></span>
            </a>
            <div class="perfil-directores-hero__plataforma-wrap" data-perfil-hero-plataforma-wrap>
              <button
                type="button"
                class="btn-base btn-secondary btn-cta-animated perfil-directores-hero__plataforma-btn"
                data-perfil-hero-plataforma-btn
                aria-expanded="false"
                aria-haspopup="true"
                aria-controls="perfil-directores-plataforma-panel"
                id="perfil-directores-plataforma-btn"
              >
                <span>Ingresa a la plataforma</span>
                <span class="perfil-directores-hero__btn-icon perfil-directores-hero__btn-icon--person" aria-hidden="true"></span>
              </button>
              <div
                class="perfil-directores-hero__mega"
                id="perfil-directores-plataforma-panel"
                role="region"
                aria-labelledby="perfil-directores-plataforma-btn"
              >
                <ul class="perfil-directores-hero__mega-list">
                  <!-- <li>
                    <a
                      class="perfil-directores-hero__mega-link"
                      href=""
                      target="_blank"
                      rel="noopener noreferrer">Preescolar</a>
                  </li> -->
                  <li>
                    <a
                      class="perfil-directores-hero__mega-link"
                      href="https://app.lucaedu.com/"
                      target="_blank"
                      rel="noopener noreferrer">Primaria</a>
                  </li>
                  <li>
                    <a
                      class="perfil-directores-hero__mega-link"
                      href="https://hs.app.lucaedu.com/"
                      target="_blank"
                      rel="noopener noreferrer">Secundaria</a>
                  </li>
                </ul>
              </div>
            </div>
          </div>
        </header>
        <div class="perfil-directores-hero__media">
          <img
            class="perfil-directores-hero__image"
            src={heroImageSrc}
            alt="Director escolar con carpeta de documentos"
            width="500"
            height="560"
            loading="eager"
            decoding="async"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  (function () {
    function closeAllPerfilHeroPlataforma() {
      document.querySelectorAll('[data-perfil-hero-plataforma-wrap]').forEach(function (w) {
        w.classList.remove('is-open');
        var b = w.querySelector('[data-perfil-hero-plataforma-btn]');
        if (b) b.setAttribute('aria-expanded', 'false');
      });
    }

    function initPerfilHeroPlataforma() {
      document.querySelectorAll('[data-perfil-hero-plataforma-wrap]').forEach(function (wrap) {
        if (wrap.dataset.perfilHeroPlataformaBound === 'true') return;
        wrap.dataset.perfilHeroPlataformaBound = 'true';
        var btn = wrap.querySelector('[data-perfil-hero-plataforma-btn]');
        if (!(btn instanceof HTMLButtonElement)) return;

        var isMobile = function () {
          return window.matchMedia('(max-width: 1100px)').matches;
        };

        btn.addEventListener('click', function (e) {
          e.stopPropagation();
          if (!isMobile()) return;
          var willOpen = !wrap.classList.contains('is-open');
          closeAllPerfilHeroPlataforma();
          if (willOpen) {
            wrap.classList.add('is-open');
            btn.setAttribute('aria-expanded', 'true');
          }
        });
      });

      if (!window.__lucaPerfilHeroPlataformaDoc) {
        window.__lucaPerfilHeroPlataformaDoc = true;
        document.addEventListener('click', function (e) {
          var t = e.target;
          if (!(t instanceof Node)) return;
          document.querySelectorAll('[data-perfil-hero-plataforma-wrap]').forEach(function (w) {
            if (!w.contains(t)) {
              w.classList.remove('is-open');
              var b = w.querySelector('[data-perfil-hero-plataforma-btn]');
              if (b) b.setAttribute('aria-expanded', 'false');
            }
          });
        });
        document.addEventListener('keydown', function (e) {
          if (e.key === 'Escape') closeAllPerfilHeroPlataforma();
        });
      }
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initPerfilHeroPlataforma);
    } else {
      initPerfilHeroPlataforma();
    }
    document.addEventListener('astro:page-load', initPerfilHeroPlataforma);
  })();
</script>

<style>
  .perfil-directores-hero {
    width: 100%;
    background-color: #ffffff;
    padding: 0;
    position: relative;
    overflow-x: hidden;
    box-sizing: border-box;
  }

  .perfil-directores-hero__strip {
    width: 85%;
    max-width: 100%;
    margin-left: auto;
    margin-right: auto;
    background-color: #d2e6ff;
    position: relative;
    overflow-x: hidden;
    overflow-y: visible;
    padding-top: calc(1rem + 64px + 128px);
    padding-bottom: clamp(2.5rem, 6vw, 4rem);
    padding-left: 0;
    padding-right: 0;
    border-radius: 0 0 32px 32px;
    box-sizing: border-box;
  }

  .perfil-directores-hero__inner {
    position: relative;
    z-index: 1;
  }

  .programas__label--align-start {
    justify-content: flex-start;
  }

  .perfil-directores-hero__content .programas__label {
    margin-bottom: 24px;
  }

  .perfil-directores-hero__row {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    align-items: center;
    width: 100%;
    box-sizing: border-box;
  }

  .perfil-directores-hero__content {
    text-align: left;
    margin: 0;
    min-width: 0;
    padding-left: 0;
    padding-right: 0;
    box-sizing: border-box;
  }

  .perfil-directores-hero__title {
    margin: 0 0 clamp(1rem, 3vw, 1.5rem);
    max-width: 950px;
    color: var(--color-black);
  }

  .perfil-directores-hero__title-dot {
    color: var(--color-red);
  }

  .perfil-directores-hero__subtitle {
    margin: 0 0 clamp(1.25rem, 4vw, 2.25rem);
    max-width: 950px;
    color: var(--color-text-base);
  }

  .perfil-directores-hero__ctas {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    justify-content: flex-start;
    gap: clamp(0.5rem, 2vw, 1rem);
    max-width: 100%;
    min-width: 0;
    overflow-x: visible;
    overflow-y: visible;
  }

  .perfil-directores-hero__ctas .btn-base {
    flex: 0 0 auto;
    white-space: nowrap;
  }

  /* Mismo patrón que Header login: hover/focus panel con 3 plataformas */
  .perfil-directores-hero__plataforma-wrap {
    position: relative;
    display: inline-flex;
    align-items: center;
    flex: 0 0 auto;
  }

  .perfil-directores-hero__mega {
    position: absolute;
    bottom: calc(100% + 12px);
    top: auto;
    left: auto;
    right: 0;
    transform: none;
    min-width: 220px;
    padding: 1rem 0;
    background-color: #edf5ff;
    border-radius: 16px;
    box-shadow: 0 8px 32px rgba(40, 48, 148, 0.12);
    z-index: 20;
  }

  .perfil-directores-hero__mega-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .perfil-directores-hero__mega-list li {
    margin: 0;
    padding: 0;
  }

  .perfil-directores-hero__mega-link {
    font-family: var(--font-family-base);
    font-weight: var(--text-body-primary-weight);
    font-size: var(--text-body-primary-size);
    letter-spacing: var(--text-body-primary-spacing);
    line-height: var(--text-body-primary-line);
    color: var(--color-blue);
    text-decoration: none;
    display: block;
    padding: 0.75rem 1.25rem;
    transition:
      color 0.2s ease,
      font-weight 0.2s ease,
      background-color 0.2s ease;
  }

  .perfil-directores-hero__mega-link:hover,
  .perfil-directores-hero__mega-link:focus {
    color: var(--color-blue-secondary);
    font-weight: var(--text-body-primary-highlight-weight);
    background-color: rgba(56, 67, 208, 0.06);
  }

  @media (min-width: 1101px) {
    .perfil-directores-hero__mega {
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
      transition: opacity 0.2s ease, visibility 0.2s ease;
    }

    .perfil-directores-hero__plataforma-wrap:hover .perfil-directores-hero__mega,
    .perfil-directores-hero__plataforma-wrap:focus-within .perfil-directores-hero__mega {
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
    }

    /* Puente invisible hacia el botón (panel arriba del CTA) */
    .perfil-directores-hero__mega::before {
      content: '';
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      height: 16px;
    }
  }

  @media (max-width: 1100px) {
    .perfil-directores-hero__mega {
      display: none;
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transition: none;
    }

    .perfil-directores-hero__mega::before {
      display: none;
    }

    .perfil-directores-hero__plataforma-wrap.is-open .perfil-directores-hero__mega {
      display: block;
    }
  }

  /* Mismo asset que .btn-terciary / Tecnología: sin círculo extra */
  .perfil-directores-hero__btn-icon {
    display: block;
    width: 1.25rem;
    height: 1.25rem;
    flex-shrink: 0;
    background-color: currentColor;
    -webkit-mask-image: url('/icons/transversal/array-right-icon.svg');
    mask-image: url('/icons/transversal/array-right-icon.svg');
    -webkit-mask-repeat: no-repeat;
    mask-repeat: no-repeat;
    -webkit-mask-position: center;
    mask-position: center;
    -webkit-mask-size: contain;
    mask-size: contain;
    transition: transform 0.2s ease;
  }

  .perfil-directores-hero__ctas .btn-base:hover .perfil-directores-hero__btn-icon {
    transform: translateX(3px);
  }

  /* Icono persona (mismo criterio que Header login) */
  .perfil-directores-hero__plataforma-btn .perfil-directores-hero__btn-icon--person {
    -webkit-mask-image: url('/icons/transversal/person-icon.svg');
    mask-image: url('/icons/transversal/person-icon.svg');
  }

  .perfil-directores-hero__plataforma-btn:hover .perfil-directores-hero__btn-icon--person {
    transform: none;
  }

  .perfil-directores-hero__media {
    position: relative;
    z-index: 1;
    min-width: 0;
    width: 100%;
    display: flex;
    justify-content: flex-end;
    align-items: flex-end;
  }

  .perfil-directores-hero__image {
    width: auto;
    max-width: min(500px, 100%);
    height: auto;
    max-height: clamp(260px, 52vw, 560px);
    display: block;
    object-fit: contain;
    object-position: right bottom;
    margin-left: auto;
    margin-right: 0;
  }

  @media (min-width: 1101px) {

    /* Más aire para copy + CTAs en una sola fila; columna fija 420px para la imagen */
    .perfil-directores-hero__row {
      grid-template-columns: minmax(0, 1fr) 420px;
      gap: clamp(1.25rem, 3vw, 2.25rem);
      align-items: center;
    }

    .perfil-directores-hero__content {
      min-width: 0;
      padding-right: 0;
    }

    .perfil-directores-hero__title {
      margin-bottom: clamp(1rem, 2.5vw, 1.5rem);
    }

    .perfil-directores-hero__subtitle {
      margin-bottom: clamp(1.25rem, 3.5vw, 2.25rem);
    }

    .perfil-directores-hero__ctas {
      flex-wrap: nowrap;
      overflow-x: visible;
    }

    /* Imagen 420px al borde derecho del strip (entra en el padding lateral del layout-container) */
    .perfil-directores-hero__media {
      width: calc(100% + var(--layout-padding-x));
      max-width: calc(420px + var(--layout-padding-x));
      margin-right: calc(-1 * var(--layout-padding-x));
      justify-content: flex-end;
      align-items: flex-end;
    }

    .perfil-directores-hero__image {
      width: 100%;
      max-width: 420px;
      max-height: clamp(320px, 42vw, 560px);
    }
  }

  @media (max-width: 1100px) {
    .perfil-directores-hero__strip {
      width: 100%;
      max-width: none;
      margin-left: 0;
      margin-right: 0;
      border-radius: 0 0 28px 28px;
      /* Pastilla fija (~0.5rem + 64px + 0.5rem) + ~80px hasta el label */
      padding-top: calc(0.5rem + 64px + 0.5rem + 80px);
    }

    .perfil-directores-hero__inner.layout-container {
      max-width: none;
      padding-left: 0;
      padding-right: 0;
    }

    .perfil-directores-hero__row {
      align-items: start;
    }

    /* Copy + CTAs: 16px laterales (coincide con pedido de márgenes al texto) */
    .perfil-directores-hero__content {
      padding-left: 24px;
      padding-right: 24px;
    }

    /* Menos aire entre etiqueta "Directores" y el H1 */
    .perfil-directores-hero__content .programas__label {
      margin-bottom: 0.5rem;
    }

    .perfil-directores-hero__title,
    .perfil-directores-hero__subtitle {
      max-width: none;
    }

    .perfil-directores-hero__title {
      margin-bottom: clamp(1rem, 3vw, 1.25rem);
    }

    .perfil-directores-hero__subtitle {
      margin-bottom: clamp(1rem, 3vw, 1.5rem);
    }

    /* CTAs en columna, ancho del bloque de copy (respeta padding 16px del header) */
    .perfil-directores-hero__ctas {
      flex-direction: column;
      align-items: stretch;
      flex-wrap: nowrap;
      width: 100%;
      max-width: none;
      margin-left: 0;
      margin-right: 0;
      overflow-x: visible;
      gap: 0.75rem;
      box-sizing: border-box;
    }

    .perfil-directores-hero__ctas .btn-base {
      flex: 1 1 auto;
      width: 100%;
      max-width: none;
      white-space: normal;
      box-sizing: border-box;
    }

    /* Imagen a ancho de franja (inner sin padding lateral en móvil) */
    .perfil-directores-hero__media {
      width: 100%;
      max-width: none;
      margin-left: 0;
      margin-right: 0;
      justify-content: flex-end;
      align-items: flex-end;
    }

    .perfil-directores-hero__image {
      width: 100%;
      max-width: none;
      height: auto;
      max-height: clamp(300px, 92vw, 460px);
      object-fit: contain;
      object-position: right bottom;
      margin-left: 0;
      margin-right: 0;
    }
  }
</style>
